=form_for Song.new, method: :post, url:"/songs/show" do |f|
  h4 Song file:
  =f.file_field :songfile
  h4 Artist:
  =f.text_field :artist
  h4 Album:
  =f.text_field :album
  h4 Song name:
  =f.text_field :name
  =f.hidden_field :user_id, value: current_user.id
  button.btn.btn-primary Submit
